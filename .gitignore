# Локальные секреты и данные — НЕ коммитим.
.env
*.env
!.env.example

# Данные рантайма (SQLite, ключ шифрования, bootstrap).
data/
*.db
*.db-wal
*.db-shm
secret.key
bootstrap.json

# Сборочные артефакты.
/bot
/remnabot.exe
/.data/
.botdata/
/out
.gotool/

# Внутренние заметки/инструкции/отчёты — НЕ коммитим.
/.md/
